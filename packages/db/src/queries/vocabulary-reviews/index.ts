import { getDueVocabularies } from "./get-due-vocabularies";
import { getStats } from "./get-stats";
import { submitReviewGrade } from "./submit-review-grade";

export const vocabularyReviewsQueries = {
	getDueVocabularies,
	getStats,
	submitReviewGrade,
};
